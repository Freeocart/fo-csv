{
  "name": "foc_csv",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "gulp watch:files",
    "dev:build": "npm run build:frontend && gulp copy:files",
    "pack": "gulp pack:files",
    "prebuild": "yarn run --cwd=./frontend/foc_csv build --prod",
    "build": "gulp build",
    "build:frontend": "yarn run --cwd=./frontend/foc_csv build"
  },
  "dependencies": {
    "gulp": "^4.0.2",
    "gulp-zip": "^5.0.2"
  },
  "devDependencies": {
    "yarn": "^1.22.10"
  }
}
